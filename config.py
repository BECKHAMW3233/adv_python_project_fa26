"""Recommendation weights and other settings."""

# Project settings, not official FTCC values -- per the assignment spec, these are stored
# separately from the converted program data rather than merged into it.
RECOMMENDATION_WEIGHTS = {
    "major_required": 3,
    "major_choice": 2,
    "general_education_choice": 1,
}
